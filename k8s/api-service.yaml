apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: microservices
spec:
  type: ClusterIP
  selector:
    app: api-service
  ports:
    - name: http
      port: 5001        # Service port inside cluster
      targetPort: 5001  # Container port